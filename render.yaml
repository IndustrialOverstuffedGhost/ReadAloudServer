services:
  - type: web
    name: readaloudserver
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: ELEVENLABS_API_KEY
        sync: false
      - key: PORT
        value: 10000
